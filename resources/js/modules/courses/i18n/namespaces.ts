import type { Namespace } from '@/common/i18n/types';

export const COURSES_NAMESPACES = {
  actions: 'actions' as Namespace,
  form: 'form' as Namespace,
  shared: 'shared' as Namespace,
  sections: 'sections' as Namespace,
  translations: 'translations' as Namespace,
} as const;
