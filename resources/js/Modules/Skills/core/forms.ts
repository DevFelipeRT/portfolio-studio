export type SkillFormData = {
    name: string;
    locale: string;
    skill_category_id: number | '';
};

export type SkillCategoryFormData = {
    name: string;
    slug: string;
    locale: string;
};
