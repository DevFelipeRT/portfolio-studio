import type { TranslationTree } from '../../types';

const themeToggle: TranslationTree = {
    label: 'Theme',
    light: 'Light',
    dark: 'Dark',
    system: 'System',
};

export default themeToggle;
